import * as React from 'react';
import { ProgressBar, MD3Colors } from 'react-native-paper';

const CustomProgressBar = () => (
  <ProgressBar animatedValue={0.5} color={"#008AD8"} />
);

export default CustomProgressBar;